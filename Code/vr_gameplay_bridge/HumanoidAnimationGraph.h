#pragma once

// Compatibility include retained for downstream local patches. New bridge code
// resolves descriptors through AnimationGraphDescriptors rather than assuming a
// humanoid graph.
#include "AnimationGraphDescriptors.h"
